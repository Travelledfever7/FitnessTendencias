services:
  postgres:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pepeperoni123
      POSTGRES_DB: fitnessaas
    ports:
      - "5432:5432"
volumes:
  postgres_data:
